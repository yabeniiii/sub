[package]
name = "protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
postcard = "1.1.3"
postcard-rpc = "0.12.1"
serde = { version = "1.0.229", default-features = false, features = ["derive"] }
